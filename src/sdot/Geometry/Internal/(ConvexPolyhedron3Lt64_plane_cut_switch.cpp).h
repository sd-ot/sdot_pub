static void *dispatch_table[] = {
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_11,
    &&case_23,
    &&case_47,
    &&case_95,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_12,
    &&case_24,
    &&case_48,
    &&case_96,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_7,
    &&case_7,
    &&case_7,
    &&case_7,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_13,
    &&case_25,
    &&case_49,
    &&case_97,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_9,
    &&case_9,
    &&case_9,
    &&case_9,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_10,
    &&case_14,
    &&case_26,
    &&case_50,
    &&case_98,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_10,
    &&case_15,
    &&case_15,
    &&case_15,
    &&case_15,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_16,
    &&case_27,
    &&case_51,
    &&case_99,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_17,
    &&case_17,
    &&case_17,
    &&case_17,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_18,
    &&case_28,
    &&case_52,
    &&case_100,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_19,
    &&case_19,
    &&case_19,
    &&case_19,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_20,
    &&case_29,
    &&case_53,
    &&case_101,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_21,
    &&case_21,
    &&case_21,
    &&case_21,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_4,
    &&case_22,
    &&case_30,
    &&case_54,
    &&case_102,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_22,
    &&case_31,
    &&case_31,
    &&case_31,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_21,
    &&case_32,
    &&case_55,
    &&case_103,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_20,
    &&case_33,
    &&case_33,
    &&case_33,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_19,
    &&case_34,
    &&case_56,
    &&case_104,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_18,
    &&case_35,
    &&case_35,
    &&case_35,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_17,
    &&case_36,
    &&case_57,
    &&case_105,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_16,
    &&case_37,
    &&case_37,
    &&case_37,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_10,
    &&case_15,
    &&case_38,
    &&case_58,
    &&case_106,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_10,
    &&case_14,
    &&case_39,
    &&case_39,
    &&case_39,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_9,
    &&case_40,
    &&case_59,
    &&case_107,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_13,
    &&case_41,
    &&case_41,
    &&case_41,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_7,
    &&case_42,
    &&case_60,
    &&case_108,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_12,
    &&case_43,
    &&case_43,
    &&case_43,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_44,
    &&case_61,
    &&case_109,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_11,
    &&case_45,
    &&case_45,
    &&case_45,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_4,
    &&case_4,
    &&case_46,
    &&case_62,
    &&case_110,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_46,
    &&case_63,
    &&case_63,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_11,
    &&case_45,
    &&case_64,
    &&case_111,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_44,
    &&case_65,
    &&case_65,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_12,
    &&case_43,
    &&case_66,
    &&case_112,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_7,
    &&case_42,
    &&case_67,
    &&case_67,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_13,
    &&case_41,
    &&case_68,
    &&case_113,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_9,
    &&case_40,
    &&case_69,
    &&case_69,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_10,
    &&case_14,
    &&case_39,
    &&case_70,
    &&case_114,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_10,
    &&case_15,
    &&case_38,
    &&case_71,
    &&case_71,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_16,
    &&case_37,
    &&case_72,
    &&case_115,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_17,
    &&case_36,
    &&case_73,
    &&case_73,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_18,
    &&case_35,
    &&case_74,
    &&case_116,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_19,
    &&case_34,
    &&case_75,
    &&case_75,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_20,
    &&case_33,
    &&case_76,
    &&case_117,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_21,
    &&case_32,
    &&case_77,
    &&case_77,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_4,
    &&case_22,
    &&case_31,
    &&case_78,
    &&case_118,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_22,
    &&case_30,
    &&case_79,
    &&case_79,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_21,
    &&case_21,
    &&case_80,
    &&case_119,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_20,
    &&case_29,
    &&case_81,
    &&case_81,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_19,
    &&case_19,
    &&case_82,
    &&case_120,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_18,
    &&case_28,
    &&case_83,
    &&case_83,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_17,
    &&case_17,
    &&case_84,
    &&case_121,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_16,
    &&case_27,
    &&case_85,
    &&case_85,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_10,
    &&case_15,
    &&case_15,
    &&case_86,
    &&case_122,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_10,
    &&case_14,
    &&case_26,
    &&case_87,
    &&case_87,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_9,
    &&case_9,
    &&case_88,
    &&case_123,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_13,
    &&case_25,
    &&case_89,
    &&case_89,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_7,
    &&case_7,
    &&case_90,
    &&case_124,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_12,
    &&case_24,
    &&case_91,
    &&case_91,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_92,
    &&case_125,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_11,
    &&case_23,
    &&case_93,
    &&case_93,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_4,
    &&case_4,
    &&case_4,
    &&case_94,
    &&case_126,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_94,
    &&case_127,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_11,
    &&case_23,
    &&case_93,
    &&case_128,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_92,
    &&case_129,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_12,
    &&case_24,
    &&case_91,
    &&case_130,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_7,
    &&case_7,
    &&case_90,
    &&case_131,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_13,
    &&case_25,
    &&case_89,
    &&case_132,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_9,
    &&case_9,
    &&case_88,
    &&case_133,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_10,
    &&case_14,
    &&case_26,
    &&case_87,
    &&case_134,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_10,
    &&case_15,
    &&case_15,
    &&case_86,
    &&case_135,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_16,
    &&case_27,
    &&case_85,
    &&case_136,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_17,
    &&case_17,
    &&case_84,
    &&case_137,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_18,
    &&case_28,
    &&case_83,
    &&case_138,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_19,
    &&case_19,
    &&case_82,
    &&case_139,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_20,
    &&case_29,
    &&case_81,
    &&case_140,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_21,
    &&case_21,
    &&case_80,
    &&case_141,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_4,
    &&case_22,
    &&case_30,
    &&case_79,
    &&case_142,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_22,
    &&case_31,
    &&case_78,
    &&case_143,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_21,
    &&case_32,
    &&case_77,
    &&case_144,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_20,
    &&case_33,
    &&case_76,
    &&case_145,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_19,
    &&case_34,
    &&case_75,
    &&case_146,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_18,
    &&case_35,
    &&case_74,
    &&case_147,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_17,
    &&case_36,
    &&case_73,
    &&case_148,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_16,
    &&case_37,
    &&case_72,
    &&case_149,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_10,
    &&case_15,
    &&case_38,
    &&case_71,
    &&case_150,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_10,
    &&case_14,
    &&case_39,
    &&case_70,
    &&case_151,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_9,
    &&case_40,
    &&case_69,
    &&case_152,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_13,
    &&case_41,
    &&case_68,
    &&case_153,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_7,
    &&case_42,
    &&case_67,
    &&case_154,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_12,
    &&case_43,
    &&case_66,
    &&case_155,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_44,
    &&case_65,
    &&case_156,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_11,
    &&case_45,
    &&case_64,
    &&case_157,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_4,
    &&case_4,
    &&case_46,
    &&case_63,
    &&case_158,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_46,
    &&case_62,
    &&case_159,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_11,
    &&case_45,
    &&case_45,
    &&case_160,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_44,
    &&case_61,
    &&case_161,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_12,
    &&case_43,
    &&case_43,
    &&case_162,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_7,
    &&case_42,
    &&case_60,
    &&case_163,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_13,
    &&case_41,
    &&case_41,
    &&case_164,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_9,
    &&case_40,
    &&case_59,
    &&case_165,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_10,
    &&case_14,
    &&case_39,
    &&case_39,
    &&case_166,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_10,
    &&case_15,
    &&case_38,
    &&case_58,
    &&case_167,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_16,
    &&case_37,
    &&case_37,
    &&case_168,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_17,
    &&case_36,
    &&case_57,
    &&case_169,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_18,
    &&case_35,
    &&case_35,
    &&case_170,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_19,
    &&case_34,
    &&case_56,
    &&case_171,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_20,
    &&case_33,
    &&case_33,
    &&case_172,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_21,
    &&case_32,
    &&case_55,
    &&case_173,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_4,
    &&case_22,
    &&case_31,
    &&case_31,
    &&case_174,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_22,
    &&case_30,
    &&case_54,
    &&case_175,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_21,
    &&case_21,
    &&case_21,
    &&case_176,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_20,
    &&case_29,
    &&case_53,
    &&case_177,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_19,
    &&case_19,
    &&case_19,
    &&case_178,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_18,
    &&case_28,
    &&case_52,
    &&case_179,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_17,
    &&case_17,
    &&case_17,
    &&case_180,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_16,
    &&case_27,
    &&case_51,
    &&case_181,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_10,
    &&case_15,
    &&case_15,
    &&case_15,
    &&case_182,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_10,
    &&case_14,
    &&case_26,
    &&case_50,
    &&case_183,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_9,
    &&case_9,
    &&case_9,
    &&case_184,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_13,
    &&case_25,
    &&case_49,
    &&case_185,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_7,
    &&case_7,
    &&case_7,
    &&case_186,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_12,
    &&case_24,
    &&case_48,
    &&case_187,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_188,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_11,
    &&case_23,
    &&case_47,
    &&case_189,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_4,
    &&case_4,
    &&case_4,
    &&case_4,
    &&case_190,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_190,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_11,
    &&case_23,
    &&case_47,
    &&case_189,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_188,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_12,
    &&case_24,
    &&case_48,
    &&case_187,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_7,
    &&case_7,
    &&case_7,
    &&case_186,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_13,
    &&case_25,
    &&case_49,
    &&case_185,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_9,
    &&case_9,
    &&case_9,
    &&case_184,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_10,
    &&case_14,
    &&case_26,
    &&case_50,
    &&case_183,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_10,
    &&case_15,
    &&case_15,
    &&case_15,
    &&case_182,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_16,
    &&case_27,
    &&case_51,
    &&case_181,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_17,
    &&case_17,
    &&case_17,
    &&case_180,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_18,
    &&case_28,
    &&case_52,
    &&case_179,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_19,
    &&case_19,
    &&case_19,
    &&case_178,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_20,
    &&case_29,
    &&case_53,
    &&case_177,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_21,
    &&case_21,
    &&case_21,
    &&case_176,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_4,
    &&case_22,
    &&case_30,
    &&case_54,
    &&case_175,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_22,
    &&case_31,
    &&case_31,
    &&case_174,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_21,
    &&case_32,
    &&case_55,
    &&case_173,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_20,
    &&case_33,
    &&case_33,
    &&case_172,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_19,
    &&case_34,
    &&case_56,
    &&case_171,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_18,
    &&case_35,
    &&case_35,
    &&case_170,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_17,
    &&case_36,
    &&case_57,
    &&case_169,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_16,
    &&case_37,
    &&case_37,
    &&case_168,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_10,
    &&case_15,
    &&case_38,
    &&case_58,
    &&case_167,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_10,
    &&case_14,
    &&case_39,
    &&case_39,
    &&case_166,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_9,
    &&case_40,
    &&case_59,
    &&case_165,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_13,
    &&case_41,
    &&case_41,
    &&case_164,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_7,
    &&case_42,
    &&case_60,
    &&case_163,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_12,
    &&case_43,
    &&case_43,
    &&case_162,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_44,
    &&case_61,
    &&case_161,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_11,
    &&case_45,
    &&case_45,
    &&case_160,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_4,
    &&case_4,
    &&case_46,
    &&case_62,
    &&case_159,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_46,
    &&case_63,
    &&case_158,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_11,
    &&case_45,
    &&case_64,
    &&case_157,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_44,
    &&case_65,
    &&case_156,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_12,
    &&case_43,
    &&case_66,
    &&case_155,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_7,
    &&case_42,
    &&case_67,
    &&case_154,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_13,
    &&case_41,
    &&case_68,
    &&case_153,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_9,
    &&case_40,
    &&case_69,
    &&case_152,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_10,
    &&case_14,
    &&case_39,
    &&case_70,
    &&case_151,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_10,
    &&case_15,
    &&case_38,
    &&case_71,
    &&case_150,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_16,
    &&case_37,
    &&case_72,
    &&case_149,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_17,
    &&case_36,
    &&case_73,
    &&case_148,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_18,
    &&case_35,
    &&case_74,
    &&case_147,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_19,
    &&case_34,
    &&case_75,
    &&case_146,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_20,
    &&case_33,
    &&case_76,
    &&case_145,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_21,
    &&case_32,
    &&case_77,
    &&case_144,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_4,
    &&case_22,
    &&case_31,
    &&case_78,
    &&case_143,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_22,
    &&case_30,
    &&case_79,
    &&case_142,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_21,
    &&case_21,
    &&case_80,
    &&case_141,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_20,
    &&case_29,
    &&case_81,
    &&case_140,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_19,
    &&case_19,
    &&case_82,
    &&case_139,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_18,
    &&case_28,
    &&case_83,
    &&case_138,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_17,
    &&case_17,
    &&case_84,
    &&case_137,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_16,
    &&case_27,
    &&case_85,
    &&case_136,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_10,
    &&case_15,
    &&case_15,
    &&case_86,
    &&case_135,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_10,
    &&case_14,
    &&case_26,
    &&case_87,
    &&case_134,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_9,
    &&case_9,
    &&case_88,
    &&case_133,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_13,
    &&case_25,
    &&case_89,
    &&case_132,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_7,
    &&case_7,
    &&case_90,
    &&case_131,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_12,
    &&case_24,
    &&case_91,
    &&case_130,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_92,
    &&case_129,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_11,
    &&case_23,
    &&case_93,
    &&case_128,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_4,
    &&case_4,
    &&case_4,
    &&case_94,
    &&case_127,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_94,
    &&case_126,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_11,
    &&case_23,
    &&case_93,
    &&case_93,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_92,
    &&case_125,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_12,
    &&case_24,
    &&case_91,
    &&case_91,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_7,
    &&case_7,
    &&case_90,
    &&case_124,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_13,
    &&case_25,
    &&case_89,
    &&case_89,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_9,
    &&case_9,
    &&case_88,
    &&case_123,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_10,
    &&case_14,
    &&case_26,
    &&case_87,
    &&case_87,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_10,
    &&case_15,
    &&case_15,
    &&case_86,
    &&case_122,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_16,
    &&case_27,
    &&case_85,
    &&case_85,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_17,
    &&case_17,
    &&case_84,
    &&case_121,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_18,
    &&case_28,
    &&case_83,
    &&case_83,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_19,
    &&case_19,
    &&case_82,
    &&case_120,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_20,
    &&case_29,
    &&case_81,
    &&case_81,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_21,
    &&case_21,
    &&case_80,
    &&case_119,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_4,
    &&case_22,
    &&case_30,
    &&case_79,
    &&case_79,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_22,
    &&case_31,
    &&case_78,
    &&case_118,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_21,
    &&case_32,
    &&case_77,
    &&case_77,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_20,
    &&case_33,
    &&case_76,
    &&case_117,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_19,
    &&case_34,
    &&case_75,
    &&case_75,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_18,
    &&case_35,
    &&case_74,
    &&case_116,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_17,
    &&case_36,
    &&case_73,
    &&case_73,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_16,
    &&case_37,
    &&case_72,
    &&case_115,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_10,
    &&case_15,
    &&case_38,
    &&case_71,
    &&case_71,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_10,
    &&case_14,
    &&case_39,
    &&case_70,
    &&case_114,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_9,
    &&case_40,
    &&case_69,
    &&case_69,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_13,
    &&case_41,
    &&case_68,
    &&case_113,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_7,
    &&case_42,
    &&case_67,
    &&case_67,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_12,
    &&case_43,
    &&case_66,
    &&case_112,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_44,
    &&case_65,
    &&case_65,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_11,
    &&case_45,
    &&case_64,
    &&case_111,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_4,
    &&case_4,
    &&case_46,
    &&case_63,
    &&case_63,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_46,
    &&case_62,
    &&case_110,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_11,
    &&case_45,
    &&case_45,
    &&case_45,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_44,
    &&case_61,
    &&case_109,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_12,
    &&case_43,
    &&case_43,
    &&case_43,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_7,
    &&case_42,
    &&case_60,
    &&case_108,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_13,
    &&case_41,
    &&case_41,
    &&case_41,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_9,
    &&case_40,
    &&case_59,
    &&case_107,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_10,
    &&case_14,
    &&case_39,
    &&case_39,
    &&case_39,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_10,
    &&case_15,
    &&case_38,
    &&case_58,
    &&case_106,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_16,
    &&case_37,
    &&case_37,
    &&case_37,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_17,
    &&case_36,
    &&case_57,
    &&case_105,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_18,
    &&case_35,
    &&case_35,
    &&case_35,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_19,
    &&case_34,
    &&case_56,
    &&case_104,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_20,
    &&case_33,
    &&case_33,
    &&case_33,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_21,
    &&case_32,
    &&case_55,
    &&case_103,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_4,
    &&case_22,
    &&case_31,
    &&case_31,
    &&case_31,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_22,
    &&case_30,
    &&case_54,
    &&case_102,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_21,
    &&case_21,
    &&case_21,
    &&case_21,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_20,
    &&case_29,
    &&case_53,
    &&case_101,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_19,
    &&case_19,
    &&case_19,
    &&case_19,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_18,
    &&case_28,
    &&case_52,
    &&case_100,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_17,
    &&case_17,
    &&case_17,
    &&case_17,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_16,
    &&case_27,
    &&case_51,
    &&case_99,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_10,
    &&case_15,
    &&case_15,
    &&case_15,
    &&case_15,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_10,
    &&case_14,
    &&case_26,
    &&case_50,
    &&case_98,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_9,
    &&case_9,
    &&case_9,
    &&case_9,
    &&case_9,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_8,
    &&case_13,
    &&case_25,
    &&case_49,
    &&case_97,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_7,
    &&case_7,
    &&case_7,
    &&case_7,
    &&case_7,
    &&case_0,
    &&case_0,
    &&case_3,
    &&case_6,
    &&case_12,
    &&case_24,
    &&case_48,
    &&case_96,
    &&case_0,
    &&case_0,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_2,
    &&case_0,
    &&case_0,
    &&case_1,
    &&case_5,
    &&case_11,
    &&case_23,
    &&case_47,
    &&case_95,
    &&case_0,
    &&case_0,
    &&case_4,
    &&case_4,
    &&case_4,
    &&case_4,
    &&case_4,
    &&case_4,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
    &&case_0,
};
using std::min;
using std::max;
goto *dispatch_table[ ouf ];
case_4: {
    // remove elem
    faces_to_rem[ nb_faces_to_rem++ ] = num_face;
    faces.node_masks[ num_face ] = 0;
    break;
}
case_148: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_3, num_node_4 );
    std::size_t max_node_3 = max( num_node_3, num_node_4 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_4, num_node_5 );
    std::size_t max_node_4 = max( num_node_4, num_node_5 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_5, num_node_6 );
    std::size_t max_node_5 = max( num_node_5, num_node_6 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_6 = min( num_node_6, num_node_7 );
    std::size_t max_node_6 = max( num_node_6, num_node_7 );
    std::size_t num_edge_6 = max_node_6 * ( max_node_6 + 1 ) / 2 + min_node_6;
    if ( edge_num_cuts[ num_edge_6 ] != num_cut ) {
        edge_num_cuts[ num_edge_6 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_6 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_7 = min( num_node_7, num_node_0 );
    std::size_t max_node_7 = max( num_node_7, num_node_0 );
    std::size_t num_edge_7 = max_node_7 * ( max_node_7 + 1 ) / 2 + min_node_7;
    if ( edge_num_cuts[ num_edge_7 ] != num_cut ) {
        edge_num_cuts[ num_edge_7 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_7 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_169: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_3, num_node_4 );
    std::size_t max_node_3 = max( num_node_3, num_node_4 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_4, num_node_5 );
    std::size_t max_node_4 = max( num_node_4, num_node_5 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_6, num_node_7 );
    std::size_t max_node_5 = max( num_node_6, num_node_7 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_137: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_3, num_node_4 );
    std::size_t max_node_3 = max( num_node_3, num_node_4 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_5, num_node_6 );
    std::size_t max_node_4 = max( num_node_5, num_node_6 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_6, num_node_7 );
    std::size_t max_node_5 = max( num_node_6, num_node_7 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_121: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_3, num_node_4 );
    std::size_t max_node_3 = max( num_node_3, num_node_4 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_5, num_node_6 );
    std::size_t max_node_4 = max( num_node_5, num_node_6 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_153: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_5 );
    std::size_t max_node_3 = max( num_node_4, num_node_5 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_5, num_node_6 );
    std::size_t max_node_4 = max( num_node_5, num_node_6 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_6, num_node_7 );
    std::size_t max_node_5 = max( num_node_6, num_node_7 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_113: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_5 );
    std::size_t max_node_3 = max( num_node_4, num_node_5 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_5, num_node_6 );
    std::size_t max_node_4 = max( num_node_5, num_node_6 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_164: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_5 );
    std::size_t max_node_3 = max( num_node_4, num_node_5 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_6, num_node_7 );
    std::size_t max_node_4 = max( num_node_6, num_node_7 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_145: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_5 );
    std::size_t max_node_3 = max( num_node_4, num_node_5 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_5, num_node_6 );
    std::size_t max_node_4 = max( num_node_5, num_node_6 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_6, num_node_7 );
    std::size_t max_node_5 = max( num_node_6, num_node_7 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_117: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_5 );
    std::size_t max_node_3 = max( num_node_4, num_node_5 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_5, num_node_6 );
    std::size_t max_node_4 = max( num_node_5, num_node_6 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_172: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_5 );
    std::size_t max_node_3 = max( num_node_4, num_node_5 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_6, num_node_7 );
    std::size_t max_node_4 = max( num_node_6, num_node_7 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_140: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_6 );
    std::size_t max_node_3 = max( num_node_5, num_node_6 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_6, num_node_7 );
    std::size_t max_node_4 = max( num_node_6, num_node_7 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_149: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_5 );
    std::size_t max_node_3 = max( num_node_4, num_node_5 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_5, num_node_6 );
    std::size_t max_node_4 = max( num_node_5, num_node_6 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_6, num_node_7 );
    std::size_t max_node_5 = max( num_node_6, num_node_7 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_115: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_5 );
    std::size_t max_node_3 = max( num_node_4, num_node_5 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_5, num_node_6 );
    std::size_t max_node_4 = max( num_node_5, num_node_6 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_168: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_5 );
    std::size_t max_node_3 = max( num_node_4, num_node_5 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_6, num_node_7 );
    std::size_t max_node_4 = max( num_node_6, num_node_7 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_136: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_6 );
    std::size_t max_node_3 = max( num_node_5, num_node_6 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_6, num_node_7 );
    std::size_t max_node_4 = max( num_node_6, num_node_7 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_152: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_6 );
    std::size_t max_node_3 = max( num_node_5, num_node_6 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_6, num_node_7 );
    std::size_t max_node_4 = max( num_node_6, num_node_7 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_165: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_7 );
    std::size_t max_node_3 = max( num_node_6, num_node_7 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_116: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_5 );
    std::size_t max_node_3 = max( num_node_4, num_node_5 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_5, num_node_6 );
    std::size_t max_node_4 = max( num_node_5, num_node_6 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_170: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_5 );
    std::size_t max_node_3 = max( num_node_4, num_node_5 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_6, num_node_7 );
    std::size_t max_node_4 = max( num_node_6, num_node_7 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_138: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_6 );
    std::size_t max_node_3 = max( num_node_5, num_node_6 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_6, num_node_7 );
    std::size_t max_node_4 = max( num_node_6, num_node_7 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_154: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_6 );
    std::size_t max_node_3 = max( num_node_5, num_node_6 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_6, num_node_7 );
    std::size_t max_node_4 = max( num_node_6, num_node_7 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_146: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_6 );
    std::size_t max_node_3 = max( num_node_5, num_node_6 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_6, num_node_7 );
    std::size_t max_node_4 = max( num_node_6, num_node_7 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_120: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_73: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_3, num_node_4 );
    std::size_t max_node_3 = max( num_node_3, num_node_4 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_4, num_node_5 );
    std::size_t max_node_4 = max( num_node_4, num_node_5 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_5, num_node_6 );
    std::size_t max_node_5 = max( num_node_5, num_node_6 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_57: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_3, num_node_4 );
    std::size_t max_node_3 = max( num_node_3, num_node_4 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_4, num_node_5 );
    std::size_t max_node_4 = max( num_node_4, num_node_5 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_6, num_node_0 );
    std::size_t max_node_5 = max( num_node_6, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_84: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_3, num_node_4 );
    std::size_t max_node_3 = max( num_node_3, num_node_4 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_5, num_node_6 );
    std::size_t max_node_4 = max( num_node_5, num_node_6 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_6, num_node_0 );
    std::size_t max_node_5 = max( num_node_6, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_68: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_5 );
    std::size_t max_node_3 = max( num_node_4, num_node_5 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_5, num_node_6 );
    std::size_t max_node_4 = max( num_node_5, num_node_6 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_6, num_node_0 );
    std::size_t max_node_5 = max( num_node_6, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_76: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_5 );
    std::size_t max_node_3 = max( num_node_4, num_node_5 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_5, num_node_6 );
    std::size_t max_node_4 = max( num_node_5, num_node_6 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_6, num_node_0 );
    std::size_t max_node_5 = max( num_node_6, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_81: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_6 );
    std::size_t max_node_3 = max( num_node_5, num_node_6 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_72: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_5 );
    std::size_t max_node_3 = max( num_node_4, num_node_5 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_5, num_node_6 );
    std::size_t max_node_4 = max( num_node_5, num_node_6 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_6, num_node_0 );
    std::size_t max_node_5 = max( num_node_6, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_85: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_6 );
    std::size_t max_node_3 = max( num_node_5, num_node_6 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_69: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_6 );
    std::size_t max_node_3 = max( num_node_5, num_node_6 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_59: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_0 );
    std::size_t max_node_3 = max( num_node_6, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_74: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_5 );
    std::size_t max_node_3 = max( num_node_4, num_node_5 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_5, num_node_6 );
    std::size_t max_node_4 = max( num_node_5, num_node_6 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_6, num_node_0 );
    std::size_t max_node_5 = max( num_node_6, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_60: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_0 );
    std::size_t max_node_3 = max( num_node_6, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_56: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_0 );
    std::size_t max_node_3 = max( num_node_6, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_82: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_0 );
    std::size_t max_node_3 = max( num_node_6, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_105: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_3, num_node_4 );
    std::size_t max_node_3 = max( num_node_3, num_node_4 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_4, num_node_5 );
    std::size_t max_node_4 = max( num_node_4, num_node_5 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_107: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_108: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_104: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_36: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_3, num_node_4 );
    std::size_t max_node_3 = max( num_node_3, num_node_4 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_4, num_node_5 );
    std::size_t max_node_4 = max( num_node_4, num_node_5 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_5, num_node_0 );
    std::size_t max_node_5 = max( num_node_5, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_41: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_5 );
    std::size_t max_node_3 = max( num_node_4, num_node_5 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_33: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_5 );
    std::size_t max_node_3 = max( num_node_4, num_node_5 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_29: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_0 );
    std::size_t max_node_3 = max( num_node_5, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_37: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_5 );
    std::size_t max_node_3 = max( num_node_4, num_node_5 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_27: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_0 );
    std::size_t max_node_3 = max( num_node_5, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_40: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_0 );
    std::size_t max_node_3 = max( num_node_5, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_28: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_0 );
    std::size_t max_node_3 = max( num_node_5, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_42: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_0 );
    std::size_t max_node_3 = max( num_node_5, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_34: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_0 );
    std::size_t max_node_3 = max( num_node_5, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_180: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_3, num_node_4 );
    std::size_t max_node_3 = max( num_node_3, num_node_4 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_6, num_node_7 );
    std::size_t max_node_4 = max( num_node_6, num_node_7 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_177: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_7 );
    std::size_t max_node_3 = max( num_node_6, num_node_7 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_181: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_7 );
    std::size_t max_node_3 = max( num_node_6, num_node_7 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_178: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_6, num_node_7 );
    std::size_t max_node_2 = max( num_node_6, num_node_7 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_53: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_0 );
    std::size_t max_node_3 = max( num_node_6, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_51: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_0 );
    std::size_t max_node_3 = max( num_node_6, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_52: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_0 );
    std::size_t max_node_3 = max( num_node_6, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_101: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_99: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_100: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_17: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_3, num_node_4 );
    std::size_t max_node_3 = max( num_node_3, num_node_4 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_13: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_0 );
    std::size_t max_node_3 = max( num_node_4, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_20: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_0 );
    std::size_t max_node_3 = max( num_node_4, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_16: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_0 );
    std::size_t max_node_3 = max( num_node_4, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_18: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_0 );
    std::size_t max_node_3 = max( num_node_4, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_132: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_6 );
    std::size_t max_node_3 = max( num_node_5, num_node_6 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_6, num_node_7 );
    std::size_t max_node_4 = max( num_node_6, num_node_7 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_133: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_7 );
    std::size_t max_node_3 = max( num_node_6, num_node_7 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_123: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_124: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_89: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_6 );
    std::size_t max_node_3 = max( num_node_5, num_node_6 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_88: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_0 );
    std::size_t max_node_3 = max( num_node_6, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_90: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_0 );
    std::size_t max_node_3 = max( num_node_6, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_25: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_0 );
    std::size_t max_node_3 = max( num_node_5, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_185: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_7 );
    std::size_t max_node_3 = max( num_node_6, num_node_7 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_184: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_6, num_node_7 );
    std::size_t max_node_2 = max( num_node_6, num_node_7 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_186: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_6, num_node_7 );
    std::size_t max_node_2 = max( num_node_6, num_node_7 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_49: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_0 );
    std::size_t max_node_3 = max( num_node_6, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_97: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_8: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_2, num_node_3 );
    std::size_t max_node_2 = max( num_node_2, num_node_3 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_3, num_node_0 );
    std::size_t max_node_3 = max( num_node_3, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_9: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_6: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_0 );
    std::size_t max_node_1 = max( num_node_3, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_156: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_6 );
    std::size_t max_node_3 = max( num_node_5, num_node_6 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_6, num_node_7 );
    std::size_t max_node_4 = max( num_node_6, num_node_7 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_161: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_7 );
    std::size_t max_node_3 = max( num_node_6, num_node_7 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_112: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_5 );
    std::size_t max_node_1 = max( num_node_4, num_node_5 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_162: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_5 );
    std::size_t max_node_1 = max( num_node_4, num_node_5 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_6, num_node_7 );
    std::size_t max_node_2 = max( num_node_6, num_node_7 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_65: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_6 );
    std::size_t max_node_3 = max( num_node_5, num_node_6 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_61: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_0 );
    std::size_t max_node_3 = max( num_node_6, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_66: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_5 );
    std::size_t max_node_1 = max( num_node_4, num_node_5 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_0 );
    std::size_t max_node_3 = max( num_node_6, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_109: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_44: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_0 );
    std::size_t max_node_3 = max( num_node_5, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_12: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_0 );
    std::size_t max_node_1 = max( num_node_4, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_129: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_7 );
    std::size_t max_node_3 = max( num_node_6, num_node_7 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_125: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_130: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_5, num_node_6 );
    std::size_t max_node_1 = max( num_node_5, num_node_6 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_6, num_node_7 );
    std::size_t max_node_2 = max( num_node_6, num_node_7 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_92: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_0 );
    std::size_t max_node_3 = max( num_node_6, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_24: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_5, num_node_0 );
    std::size_t max_node_1 = max( num_node_5, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_188: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_6, num_node_7 );
    std::size_t max_node_2 = max( num_node_6, num_node_7 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_48: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_6, num_node_0 );
    std::size_t max_node_1 = max( num_node_6, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_96: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_7, num_node_0 );
    std::size_t max_node_1 = max( num_node_7, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_2: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_1, num_node_2 );
    std::size_t max_node_1 = max( num_node_1, num_node_2 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_1: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_0 );
    std::size_t max_node_1 = max( num_node_2, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_3: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_0 );
    std::size_t max_node_1 = max( num_node_2, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_144: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_6 );
    std::size_t max_node_3 = max( num_node_5, num_node_6 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_6, num_node_7 );
    std::size_t max_node_4 = max( num_node_6, num_node_7 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_173: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_7 );
    std::size_t max_node_3 = max( num_node_6, num_node_7 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_141: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_7 );
    std::size_t max_node_3 = max( num_node_6, num_node_7 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_119: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_77: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_6 );
    std::size_t max_node_3 = max( num_node_5, num_node_6 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_55: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_0 );
    std::size_t max_node_3 = max( num_node_6, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_80: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_0 );
    std::size_t max_node_3 = max( num_node_6, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_103: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_32: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_0 );
    std::size_t max_node_3 = max( num_node_5, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_176: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_6, num_node_7 );
    std::size_t max_node_2 = max( num_node_6, num_node_7 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_21: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_5: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_0 );
    std::size_t max_node_1 = max( num_node_3, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_157: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_5 );
    std::size_t max_node_1 = max( num_node_4, num_node_5 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_7 );
    std::size_t max_node_3 = max( num_node_6, num_node_7 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_111: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_5 );
    std::size_t max_node_1 = max( num_node_4, num_node_5 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_160: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_5 );
    std::size_t max_node_1 = max( num_node_4, num_node_5 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_6, num_node_7 );
    std::size_t max_node_2 = max( num_node_6, num_node_7 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_64: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_5 );
    std::size_t max_node_1 = max( num_node_4, num_node_5 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_0 );
    std::size_t max_node_3 = max( num_node_6, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_45: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_5 );
    std::size_t max_node_1 = max( num_node_4, num_node_5 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_11: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_0 );
    std::size_t max_node_1 = max( num_node_4, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_128: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_5, num_node_6 );
    std::size_t max_node_1 = max( num_node_5, num_node_6 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_6, num_node_7 );
    std::size_t max_node_2 = max( num_node_6, num_node_7 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_93: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_5, num_node_6 );
    std::size_t max_node_1 = max( num_node_5, num_node_6 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_23: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_5, num_node_0 );
    std::size_t max_node_1 = max( num_node_5, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_189: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_6, num_node_7 );
    std::size_t max_node_1 = max( num_node_6, num_node_7 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_47: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_6, num_node_0 );
    std::size_t max_node_1 = max( num_node_6, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_95: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_0, num_node_1 );
    std::size_t max_node_0 = max( num_node_0, num_node_1 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_0 );
        const Node &n1 = nodes.local_at( num_node_1 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_7, num_node_0 );
    std::size_t max_node_1 = max( num_node_7, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_150: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_6 );
    std::size_t max_node_3 = max( num_node_5, num_node_6 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_6, num_node_7 );
    std::size_t max_node_4 = max( num_node_6, num_node_7 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_7, num_node_0 );
    std::size_t max_node_5 = max( num_node_7, num_node_0 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_122: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_114: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_5 );
    std::size_t max_node_1 = max( num_node_4, num_node_5 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_166: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_5 );
    std::size_t max_node_1 = max( num_node_4, num_node_5 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_6, num_node_7 );
    std::size_t max_node_2 = max( num_node_6, num_node_7 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_58: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_0 );
    std::size_t max_node_3 = max( num_node_6, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_86: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_0 );
    std::size_t max_node_3 = max( num_node_6, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_70: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_5 );
    std::size_t max_node_1 = max( num_node_4, num_node_5 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_0 );
    std::size_t max_node_3 = max( num_node_6, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_106: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_38: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_0 );
    std::size_t max_node_3 = max( num_node_5, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_182: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_6, num_node_7 );
    std::size_t max_node_2 = max( num_node_6, num_node_7 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_14: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_0 );
    std::size_t max_node_1 = max( num_node_4, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_134: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_5, num_node_6 );
    std::size_t max_node_1 = max( num_node_5, num_node_6 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_6, num_node_7 );
    std::size_t max_node_2 = max( num_node_6, num_node_7 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_26: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_5, num_node_0 );
    std::size_t max_node_1 = max( num_node_5, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_50: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_6, num_node_0 );
    std::size_t max_node_1 = max( num_node_6, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_98: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_7, num_node_0 );
    std::size_t max_node_1 = max( num_node_7, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_10: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_0 );
    std::size_t max_node_1 = max( num_node_3, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_118: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_3, num_node_4 );
    std::size_t max_node_0 = max( num_node_3, num_node_4 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_5 );
    std::size_t max_node_1 = max( num_node_4, num_node_5 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_174: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_3, num_node_4 );
    std::size_t max_node_0 = max( num_node_3, num_node_4 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_5 );
    std::size_t max_node_1 = max( num_node_4, num_node_5 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_6, num_node_7 );
    std::size_t max_node_2 = max( num_node_6, num_node_7 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_142: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_3, num_node_4 );
    std::size_t max_node_0 = max( num_node_3, num_node_4 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_5, num_node_6 );
    std::size_t max_node_1 = max( num_node_5, num_node_6 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_6, num_node_7 );
    std::size_t max_node_2 = max( num_node_6, num_node_7 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_78: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_3, num_node_4 );
    std::size_t max_node_0 = max( num_node_3, num_node_4 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_5 );
    std::size_t max_node_1 = max( num_node_4, num_node_5 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_0 );
    std::size_t max_node_3 = max( num_node_6, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_30: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_3, num_node_4 );
    std::size_t max_node_0 = max( num_node_3, num_node_4 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_5, num_node_0 );
    std::size_t max_node_1 = max( num_node_5, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_54: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_3, num_node_4 );
    std::size_t max_node_0 = max( num_node_3, num_node_4 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_6, num_node_0 );
    std::size_t max_node_1 = max( num_node_6, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_102: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_3, num_node_4 );
    std::size_t max_node_0 = max( num_node_3, num_node_4 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_7, num_node_0 );
    std::size_t max_node_1 = max( num_node_7, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_22: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    std::size_t min_node_0 = min( num_node_3, num_node_4 );
    std::size_t max_node_0 = max( num_node_3, num_node_4 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_0 );
    std::size_t max_node_1 = max( num_node_4, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_158: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_4, num_node_5 );
    std::size_t max_node_0 = max( num_node_4, num_node_5 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_5, num_node_6 );
    std::size_t max_node_1 = max( num_node_5, num_node_6 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_6, num_node_7 );
    std::size_t max_node_2 = max( num_node_6, num_node_7 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_7, num_node_0 );
    std::size_t max_node_3 = max( num_node_7, num_node_0 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_62: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_4, num_node_5 );
    std::size_t max_node_0 = max( num_node_4, num_node_5 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_6, num_node_0 );
    std::size_t max_node_1 = max( num_node_6, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_110: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_4, num_node_5 );
    std::size_t max_node_0 = max( num_node_4, num_node_5 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_7, num_node_0 );
    std::size_t max_node_1 = max( num_node_7, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_46: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_4, num_node_5 );
    std::size_t max_node_0 = max( num_node_4, num_node_5 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_5, num_node_0 );
    std::size_t max_node_1 = max( num_node_5, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_126: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_5, num_node_6 );
    std::size_t max_node_0 = max( num_node_5, num_node_6 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_7, num_node_0 );
    std::size_t max_node_1 = max( num_node_7, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_94: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_5, num_node_6 );
    std::size_t max_node_0 = max( num_node_5, num_node_6 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_6, num_node_0 );
    std::size_t max_node_1 = max( num_node_6, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_190: {
    int num_node_0 = faces.node_lists[ num_face ][ 0 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_6, num_node_7 );
    std::size_t max_node_0 = max( num_node_6, num_node_7 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_7, num_node_0 );
    std::size_t max_node_1 = max( num_node_7, num_node_0 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_7 );
        const Node &n1 = nodes.local_at( num_node_0 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_147: {
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_5 );
    std::size_t max_node_3 = max( num_node_4, num_node_5 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_4 = min( num_node_5, num_node_6 );
    std::size_t max_node_4 = max( num_node_5, num_node_6 );
    std::size_t num_edge_4 = max_node_4 * ( max_node_4 + 1 ) / 2 + min_node_4;
    if ( edge_num_cuts[ num_edge_4 ] != num_cut ) {
        edge_num_cuts[ num_edge_4 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_4 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_5 = min( num_node_6, num_node_7 );
    std::size_t max_node_5 = max( num_node_6, num_node_7 );
    std::size_t num_edge_5 = max_node_5 * ( max_node_5 + 1 ) / 2 + min_node_5;
    if ( edge_num_cuts[ num_edge_5 ] != num_cut ) {
        edge_num_cuts[ num_edge_5 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_5 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_163: {
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_7 );
    std::size_t max_node_3 = max( num_node_6, num_node_7 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_171: {
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_7 );
    std::size_t max_node_3 = max( num_node_6, num_node_7 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_139: {
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_7 );
    std::size_t max_node_3 = max( num_node_6, num_node_7 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_83: {
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_6 );
    std::size_t max_node_3 = max( num_node_5, num_node_6 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_67: {
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_6 );
    std::size_t max_node_3 = max( num_node_5, num_node_6 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_75: {
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_6 );
    std::size_t max_node_3 = max( num_node_5, num_node_6 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_35: {
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_4, num_node_5 );
    std::size_t max_node_3 = max( num_node_4, num_node_5 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_179: {
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_3, num_node_4 );
    std::size_t max_node_2 = max( num_node_3, num_node_4 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_7 );
    std::size_t max_node_3 = max( num_node_6, num_node_7 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_19: {
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_131: {
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_7 );
    std::size_t max_node_3 = max( num_node_6, num_node_7 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_7: {
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_2, num_node_3 );
    std::size_t max_node_1 = max( num_node_2, num_node_3 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_155: {
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_5 );
    std::size_t max_node_1 = max( num_node_4, num_node_5 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_7 );
    std::size_t max_node_3 = max( num_node_6, num_node_7 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_43: {
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_5 );
    std::size_t max_node_1 = max( num_node_4, num_node_5 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_91: {
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_5, num_node_6 );
    std::size_t max_node_1 = max( num_node_5, num_node_6 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_187: {
    int num_node_1 = faces.node_lists[ num_face ][ 1 ];
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_1, num_node_2 );
    std::size_t max_node_0 = max( num_node_1, num_node_2 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_1 );
        const Node &n1 = nodes.local_at( num_node_2 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_6, num_node_7 );
    std::size_t max_node_1 = max( num_node_6, num_node_7 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_167: {
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_7 );
    std::size_t max_node_3 = max( num_node_6, num_node_7 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_135: {
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_7 );
    std::size_t max_node_3 = max( num_node_6, num_node_7 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_151: {
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_5 );
    std::size_t max_node_1 = max( num_node_4, num_node_5 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_7 );
    std::size_t max_node_3 = max( num_node_6, num_node_7 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_71: {
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_4, num_node_5 );
    std::size_t max_node_2 = max( num_node_4, num_node_5 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_5, num_node_6 );
    std::size_t max_node_3 = max( num_node_5, num_node_6 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_39: {
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_5 );
    std::size_t max_node_1 = max( num_node_4, num_node_5 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_15: {
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_3, num_node_4 );
    std::size_t max_node_1 = max( num_node_3, num_node_4 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_87: {
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_5, num_node_6 );
    std::size_t max_node_1 = max( num_node_5, num_node_6 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_183: {
    int num_node_2 = faces.node_lists[ num_face ][ 2 ];
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_2, num_node_3 );
    std::size_t max_node_0 = max( num_node_2, num_node_3 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_2 );
        const Node &n1 = nodes.local_at( num_node_3 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_6, num_node_7 );
    std::size_t max_node_1 = max( num_node_6, num_node_7 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_143: {
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_3, num_node_4 );
    std::size_t max_node_0 = max( num_node_3, num_node_4 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_5 );
    std::size_t max_node_1 = max( num_node_4, num_node_5 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_2 = min( num_node_5, num_node_6 );
    std::size_t max_node_2 = max( num_node_5, num_node_6 );
    std::size_t num_edge_2 = max_node_2 * ( max_node_2 + 1 ) / 2 + min_node_2;
    if ( edge_num_cuts[ num_edge_2 ] != num_cut ) {
        edge_num_cuts[ num_edge_2 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_2 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_3 = min( num_node_6, num_node_7 );
    std::size_t max_node_3 = max( num_node_6, num_node_7 );
    std::size_t num_edge_3 = max_node_3 * ( max_node_3 + 1 ) / 2 + min_node_3;
    if ( edge_num_cuts[ num_edge_3 ] != num_cut ) {
        edge_num_cuts[ num_edge_3 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_3 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_79: {
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_3, num_node_4 );
    std::size_t max_node_0 = max( num_node_3, num_node_4 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_5, num_node_6 );
    std::size_t max_node_1 = max( num_node_5, num_node_6 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_31: {
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    std::size_t min_node_0 = min( num_node_3, num_node_4 );
    std::size_t max_node_0 = max( num_node_3, num_node_4 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_4, num_node_5 );
    std::size_t max_node_1 = max( num_node_4, num_node_5 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_175: {
    int num_node_3 = faces.node_lists[ num_face ][ 3 ];
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_3, num_node_4 );
    std::size_t max_node_0 = max( num_node_3, num_node_4 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_3 );
        const Node &n1 = nodes.local_at( num_node_4 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_6, num_node_7 );
    std::size_t max_node_1 = max( num_node_6, num_node_7 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_159: {
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_4, num_node_5 );
    std::size_t max_node_0 = max( num_node_4, num_node_5 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_6, num_node_7 );
    std::size_t max_node_1 = max( num_node_6, num_node_7 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_63: {
    int num_node_4 = faces.node_lists[ num_face ][ 4 ];
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    std::size_t min_node_0 = min( num_node_4, num_node_5 );
    std::size_t max_node_0 = max( num_node_4, num_node_5 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_4 );
        const Node &n1 = nodes.local_at( num_node_5 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_5, num_node_6 );
    std::size_t max_node_1 = max( num_node_5, num_node_6 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_127: {
    int num_node_5 = faces.node_lists[ num_face ][ 5 ];
    int num_node_6 = faces.node_lists[ num_face ][ 6 ];
    int num_node_7 = faces.node_lists[ num_face ][ 7 ];
    std::size_t min_node_0 = min( num_node_5, num_node_6 );
    std::size_t max_node_0 = max( num_node_5, num_node_6 );
    std::size_t num_edge_0 = max_node_0 * ( max_node_0 + 1 ) / 2 + min_node_0;
    if ( edge_num_cuts[ num_edge_0 ] != num_cut ) {
        edge_num_cuts[ num_edge_0 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_0 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_5 );
        const Node &n1 = nodes.local_at( num_node_6 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    std::size_t min_node_1 = min( num_node_6, num_node_7 );
    std::size_t max_node_1 = max( num_node_6, num_node_7 );
    std::size_t num_edge_1 = max_node_1 * ( max_node_1 + 1 ) / 2 + min_node_1;
    if ( edge_num_cuts[ num_edge_1 ] != num_cut ) {
        edge_num_cuts[ num_edge_1 ] = num_cut;
        Node *node = &nodes.local_at( add_nodes_size++ );
        edge_cuts[ num_edge_1 ] = node;
        
        const Node &n0 = nodes.local_at( num_node_6 );
        const Node &n1 = nodes.local_at( num_node_7 );
        P( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
        node->set_pos( n0.pos() + n0.d / ( n0.d - n1.d ) * ( n1.pos() - n0.pos() ) );
    }
    break;
}
case_0:
    ;
